#!/bin/bash

source connector-branch query << 'EOF'
CREATE DATABASE `schemer`;

CREATE TABLE `schemer`.`schema_version`
(
	`schema` VARCHAR(255) NOT NULL,
	`version` INTEGER NOT NULL,
	`created` DATETIME NOT NULL,
	`deleted` DATETIME
);
EOF
