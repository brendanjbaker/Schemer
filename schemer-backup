#!/bin/bash

set -e

usage() {
	echo "usage: dbvm backup <command> [<arguments>]"
	echo
	echo "Supported commands are:"
	echo "create    Create backup"
	echo "restore   Restore backup"
	echo
}

if [ $# -lt 1 ]; then
	usage >&2
	exit 1
fi

command=$1

case $command in
	create)
		./dbvm-backup-create "${@:2}"
		;;

	restore)
		./dbvm-backup-restore "${@:2}"
		;;

	*)
		echo "dbvm: '$command' is not a recognized command." >&2
		echo >&2
		usage >&2
		exit 1
esac
