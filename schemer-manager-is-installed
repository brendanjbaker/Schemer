#!/bin/bash

set -e

source usage 1 "schemer manager is-installed <database-configuration-path>" "$@"

database_configuration_path=$1

source apply-configuration $database_configuration_path

source query $database_configuration_path &> /dev/null << EOF
SELECT "id" FROM "$versioning_schema"."database_version";
EOF
